describe('UserRouter', () => {
  beforeAll(done => {
    done();
  });

  describe(`Test placeholder`, () => {
    it('should succeed', done => {
      expect('cem').toEqual('cem');
      done();
    });
  });
});
